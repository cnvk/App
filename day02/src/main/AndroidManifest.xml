<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="my.microsoft.com.day02">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />

        <service
            android:name=".StartService01"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".StartServiceActivity01">

        </activity>

        <service
            android:name=".StartService02"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".StartServiceActivity02" />

        <service
            android:name=".StartService03"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".StartServiceActivity03" />

        <service
            android:name=".StartService04"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".StartServiceActivity04" />

        <service
            android:name=".StartService05"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".StartServiceActivity05">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>